// Prisma schema — the single source of truth for the database.
// After editing: npm run prisma:migrate --workspace @sakura/api

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// No models yet — add them as the domain takes shape, then run a migration.
